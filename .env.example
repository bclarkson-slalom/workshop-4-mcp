# Environment Variables for Slalom Capabilities Management

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-here-generate-with-openssl-rand-hex-32
TOKEN_EXPIRE_MINUTES=480

# Application Configuration
ENVIRONMENT=development

# Security Note:
# - NEVER commit the actual .env file with real secrets
# - Generate JWT_SECRET_KEY with: openssl rand -hex 32
# - Use different keys for dev, staging, and production
# - Keep production secrets in secure vault (AWS Secrets Manager, Azure Key Vault, etc.)
